var config = {
		baseURL : "${maven.js.config.baseURL}",
		
		simpleUploadUrl : "${maven.js.config.simpleUploadUrl}",

		storageUrl : "${maven.js.config.storageUrl}",
		
		init : function() {
			
		}
};
config.init();